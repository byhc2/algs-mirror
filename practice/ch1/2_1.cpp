#include <iostream>
#include "geometry.h"
#include "algsnum.h"

using namespace std;
using namespace algs;

int main(int argc, char *argv[])
{
    assert(argc == 2);

    Int n = Num::parseInt(argv[1]);

    while
}
